<section class="section-centred">
  <div class="wrapper wrapper-nav-padded">
    <h1 class="section-heading italic-heading">{{ page.title }}</h1>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</section>
